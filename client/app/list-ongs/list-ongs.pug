div.animated.fadeIn#listOngs
  div#pageHeader(flex="100"  layout="column" layout-align="center center" ng-style="{'background-image': 'url({{$ctrl.pageImage}})'}")
    h2 {{$ctrl.pageTitle}}
  div#mainListOngs(layout="column" flex="100")
    div#lista(flex = "100" layout="column")
      h3 Abaixo estão as ONGs que mais se destacaram em nossos critérios de avaliação.
      md-grid-list#listOngs(flex
          md-cols-gt-xs="2" 
          md-cols-gt-md="3"
          md-cols="1" 
          md-row-height="4:5"
          md-gutter-gt-sd="25px" 
            md-gutter="35px")
        md-grid-tile(ng-repeat="ong in $ctrl.listOngToDisplay")
          div(layout="column" layout-fill)
            a(ng-href="pagOngs/{{ong.slug}}")
              md-card
                md-card-title
                  md-card-title-text(layout-align="center center" layout="column")
                    h1 {{ong.nomeFantasia}}
                    img(ng-src="{{ong.logo}}" style="width: 300px; height:300px")  
                md-card-content(layout-align="center center" layout="column")
                  p {{ong.desc}}
                  md-list
                    md-list-item(ng-repeat="loc in ong.localidadesDeAtuacao")
                      h4 Atua em: {{loc.localidade}}
            div(layout="row" layout-align="center center")
              a(ng-href="pagOngs/{{ong.slug}}")
                md-button.md-raised.md-primary Apoiar
contact-form