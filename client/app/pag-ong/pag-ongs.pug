div#listOngs
  div#carousel(flex="100")
  jk-carousel(
        flex="100"
        data="$ctrl.carouselImages"
        item-template-url="'img.tmpl.pug'"
        max-width="2000" 
        max-height="1300"
        auto-slide="true"
        auto-slide-time="3000")   
  div#pageHeader(flex="100"  layout="column" layout-align="center center" ng-style="{'background-image': 'url({{$ctrl.pageImage}})'}")
    h2 {{$ctrl.pageTitle}}
  md-content#mainListOngs(layout="column" flex="100")
    md-content#lista(flex = "100" layout="column" layout-align="center center")
      h3 Abaixo estão as ONGs que mais se destacaram em nossos critérios de avaliação.
      .container
        md-list#listOngs(flex="100" layout-gt-sm="row" layout="column")
          md-list-item(ng-repeat="ong in $ctrl.listOngToDisplay " layout="column"  layout-align="center center")
            md-card
              md-card-title
                md-card-title-text(layout-align="center center")
                  h1 {{ong.nome}} 
                  img.md-card-image(ng-src="{{ong.logo}}" )
                  h4 Atua em: {{ong.localidades}}
              md-card-content
                  p {{ong.desc}}
              md-action(layout="row" layout-align="center center")
            md-button.md-raised.md-primary Apoiar
contact-form